# AI资讯助手方案设计

## 1. 项目目标

基于现有 `ai-news` 系列 skills，设计一个新的 `AI资讯助手` 产品方案。

该产品的核心目标不是继续让用户手动触发 skill，而是实现一套以 `OpenClaw` 为前台聊天入口、以 `飞书多维表格` 为流程状态中台、以 `飞书文档` 为内容承载载体的资讯处理系统。

用户最终体验应当是：

- 我只需要和 OpenClaw 聊天
- 我发资讯，它自动收录
- 我指定某条资讯，它陪我阅读和追问
- 我讨论结束，它自动整理成笔记
- 我需要传播，它自动生成口播稿或文案
- 所有内容最终沉淀在飞书里，方便查看、检索、编辑、复用

## 2. 设计原则

### 2.1 单条资讯为最小工作单元

系统中所有流程都围绕“一条资讯”展开，而不是围绕“一天的资讯合集文档”展开。

这样可以支持：

- 精准定位某条资讯进行阅读
- 针对某条资讯单独提问和联网补充
- 单条资讯独立归档成笔记
- 单条资讯独立转成口播稿或传播文案

### 2.2 OpenClaw 作为唯一用户入口

用户不再直接操作 skill，也不需要理解后台模块名称。

用户只与 OpenClaw 聊天，例如：

- “帮我收一下今天这几条资讯”
- “看第 2 条”
- “继续聊这条”
- “整理成笔记”
- “转成口播稿”

系统负责自动识别意图并推进流程。

### 2.3 飞书作为主工作界面

飞书不是单纯的同步副本，而是最终主界面。

- `飞书多维表格` 承担状态、索引、检索、流程推进
- `飞书文档` 承担正文阅读、笔记沉淀、成稿编辑

### 2.4 旧 skills 变成内部能力模块

现有 skill 依然保留价值，但角色要调整：

- 不再是用户手动触发入口
- 改为系统内部被编排调用的处理模块

## 3. 总体架构

整体架构建议如下：

`OpenClaw -> 任务编排层 -> 飞书多维表格 + 飞书文档 -> 本地能力模块`

### 3.1 OpenClaw

负责：

- 承接用户输入
- 识别当前意图
- 维持对话体验
- 在合适节点触发收录、阅读、归档、创作流程

### 3.2 任务编排层

这是整个系统的核心。

它负责：

- 判断当前消息属于哪个流程阶段
- 定位当前正在处理的资讯对象
- 查询或更新状态
- 调用对应技能模块
- 把结果写回飞书

编排层的本质不是“调用工具”，而是“推进资讯生命周期”。

### 3.3 飞书多维表格

作为流程状态中台，负责：

- 记录每条资讯的结构化元数据
- 记录每条资讯当前流程状态
- 记录资讯、笔记、口播稿之间的关联
- 支持未来筛选、排序、检索、回看

### 3.4 飞书文档

作为内容承载层，负责：

- 承载资讯正文
- 承载阅读中的背景资料和整理结果
- 承载深度笔记
- 承载口播稿和文案终稿

### 3.5 本地能力模块

本地目录和既有 skills 继续存在，但主要负责：

- 复用内容处理逻辑
- 保存模板、脚本、方法论
- 做开发调试、缓存和备份

## 4. 核心流程设计

系统建议分为 4 条主流程。

### 4.1 流程一：资讯收录

用户输入一段资讯文本、链接，或多条混合内容后，系统执行：

1. 识别输入中包含多少条资讯
2. 为每条资讯生成唯一 ID
3. 自动抽取标题、摘要、来源链接、日期
4. 自动打标签和初步分类
5. 在飞书多维表格 `资讯主表` 中新增记录
6. 写入当日飞书日报文档
7. 将状态标记为 `已收录` 或 `待阅读`

### 4.2 流程二：阅读陪伴

当用户说“看第 2 条”“聊这条资讯”时，系统执行：

1. 根据当前上下文或飞书表定位目标资讯
2. 将该资讯状态切换为 `阅读中`
3. 读取资讯正文与相关链接
4. 如用户追问超出原文范围，则自动联网补背景
5. 将对话中的关键问题、关键回答、引用来源记录下来
6. 在飞书文档中持续沉淀“认知草稿”

这一阶段的目标不是一次性输出答案，而是围绕一条资讯形成连续理解。

### 4.3 流程三：归档沉淀

当用户说“整理一下”“归档成笔记”时，系统执行：

1. 提取该条资讯的原始内容
2. 提取围绕这条资讯的阅读会话和关键问答
3. 按逻辑顺序重组为正式笔记
4. 生成独立飞书笔记文档
5. 在多维表格中回填笔记链接
6. 将资讯状态切换为 `已生成笔记`

### 4.4 流程四：内容创作

当用户说“转成口播稿”“给我一个传播版本”时，系统执行：

1. 读取对应笔记文档
2. 按指定内容方法论生成口播稿或文案
3. 输出到独立飞书文档
4. 在多维表格中建立与笔记、资讯的关联
5. 将状态更新为 `已生成文案`

## 5. 飞书多维表格设计

建议第一版采用 4 张主表。

### 5.1 资讯主表

一条资讯一条记录。

建议字段：

- `资讯ID`
- `日期`
- `标题`
- `原始内容`
- `原始链接`
- `来源类型`
- `自动标签`
- `主题分类`
- `优先级`
- `当前状态`
- `是否重点跟进`
- `日报文档链接`
- `深度笔记链接`
- `口播稿链接`
- `最后更新时间`

建议状态枚举：

- `已收录`
- `待阅读`
- `阅读中`
- `讨论中`
- `已生成笔记`
- `已生成文案`
- `已归档`

### 5.2 阅读会话表

用于记录围绕某条资讯展开了哪些讨论。

建议字段：

- `会话ID`
- `资讯ID`
- `开始时间`
- `结束时间`
- `会话状态`
- `用户关注点`
- `关键问题`
- `关键结论`
- `背景补充来源`
- `关联笔记链接`

### 5.3 笔记表

用于记录沉淀后的正式笔记。

建议字段：

- `笔记ID`
- `资讯ID`
- `会话ID`
- `笔记标题`
- `笔记主题`
- `飞书文档链接`
- `版本号`
- `是否已归档`
- `是否已转文案`

### 5.4 文案表

用于记录口播稿、短视频稿、分享稿等输出成品。

建议字段：

- `文案ID`
- `来源笔记ID`
- `来源资讯ID`
- `文案类型`
- `标题`
- `飞书文档链接`
- `版本号`
- `发布状态`
- `最后更新时间`

## 6. 飞书文档结构设计

飞书文档不建议全部堆在一个地方，而是按内容生命周期拆开。

### 6.1 总导航文档

建立一份 `AI资讯助手总导航` 文档，作为人工查看入口。

建议包含：

- 最近 7 天日报
- 最近阅读中的资讯
- 待归档条目
- 待转文案条目
- 最新笔记
- 最新口播稿

### 6.2 每日日报文档

每天一篇日报文档，用来承接当天新收录的资讯。

每条资讯以卡片形式呈现，建议字段包括：

- 标题
- 摘要
- 原始内容
- 原始链接
- 标签
- 当前状态
- 跳转入口

### 6.3 深度笔记文档

当一条资讯被深聊后，为其生成独立文档。

建议结构：

- 资讯标题
- 原始资讯摘要
- 用户关注问题
- 背景补充资料
- 讨论过程整理
- 结论与认知沉淀
- 可转创作的核心观点

### 6.4 成稿文档

每篇口播稿或传播稿独立成文。

建议结构：

- 标题
- 面向人群
- 传播目的
- 成稿正文
- 可选短标题
- 可选导语

## 7. OpenClaw 的意图识别与流程路由

OpenClaw 前台建议只暴露自然语言交互，不暴露后台 skill 名称。

系统至少需要识别 4 类意图：

### 7.1 入库意图

典型输入：

- “帮我收一下这几条资讯”
- “今天这些内容先存起来”

路由到：`资讯收录处理器`

### 7.2 阅读意图

典型输入：

- “看第 3 条”
- “我们聊这条”
- “这条到底什么意思”

路由到：`阅读陪伴处理器`

### 7.3 归档意图

典型输入：

- “整理成笔记”
- “归档吧”

路由到：`笔记归档处理器`

### 7.4 创作意图

典型输入：

- “转成口播稿”
- “给我一个短视频版本”
- “写成分享文案”

路由到：`内容生成处理器`

## 8. 现有 skills 的系统映射

现有 skills 不建议原样继续让用户手动调用，而应进行“模块化改造”。

### 8.1 `ainews`

现有能力：

- 抓取资讯
- 提取正文
- 落盘

新系统中的角色：

- `资讯输入处理器`
- `资讯抓取与清洗模块`

需要调整点：

- 从“固定 RSS 抓取”扩展为“用户输入 + 链接解析 + 可选自动抓取”
- 输出目标从本地目录改为“飞书日报 + 资讯主表”

### 8.2 `ainews-companion`

现有能力：

- 对单条资讯进行阅读陪伴
- 用户追问时做原文补充和联网背景查询
- 维护认知草稿

新系统中的角色：

- `阅读陪伴处理器`

需要调整点：

- 会话状态不能只依赖聊天上下文，要写回表格和文档
- 输出不只是即时回答，还要能沉淀为后续笔记原料

### 8.3 `ainews-archive`

现有能力：

- 把认知草稿写入 PARA
- 更新导航
- 清理资源

新系统中的角色：

- `笔记归档处理器`

需要调整点：

- 归档目标从本地 PARA 改为“飞书笔记文档 + 多维表格状态更新”

### 8.4 `XHIP-agent`

现有能力：

- 基于文章或笔记生成“晓辉博士”风格口播稿

新系统中的角色：

- `内容生成处理器`

需要调整点：

- 输入源改为飞书笔记链接或笔记正文
- 输出目标改为飞书成稿文档

## 9. MVP 范围建议

第一版不要试图做全量自动化，而要先把主流程跑通。

建议 MVP 只做以下能力：

### 9.1 要做的

- 用户输入资讯后自动拆条
- 自动写入飞书日报
- 自动登记到资讯主表
- 用户指定某条资讯后可连续阅读和追问
- 结束后生成独立笔记文档
- 基于笔记生成口播稿

### 9.2 暂时不做的

- 多来源自动抓取平台大全
- 多种复杂发布渠道联动
- 高级推荐系统
- 多用户协作权限控制
- 自动判断何时必须归档的复杂智能体自治逻辑

## 10. 风险与设计注意点

### 10.1 风险一：流程状态漂移

如果 OpenClaw 只依赖上下文记忆，很容易出现“用户当前在看哪条资讯”丢失的问题。

解决建议：

- 关键状态必须写入飞书多维表格
- 每轮操作先读状态再执行

### 10.2 风险二：飞书文档与状态表脱节

如果文档生成了，但表格没回填链接，后续流程会断裂。

解决建议：

- 将“写文档 + 回填表格”视为一个原子流程
- 失败时记录待补偿任务

### 10.3 风险三：旧 skills 耦合本地目录过重

现有 skills 明显假设了本地 PARA 结构。

解决建议：

- 把 skill 中“内容处理逻辑”和“本地目录写入逻辑”拆开
- 保留前者，替换后者

## 11. 推荐实施路线

### 阶段一：飞书化 MVP

目标：

- 跑通 `输入资讯 -> 飞书收录 -> 指定单条 -> 阅读讨论 -> 飞书笔记 -> 飞书口播稿`

这一阶段重点是验证完整链路。

### 阶段二：流程记忆增强

目标：

- 优化会话状态管理
- 优化多维表格筛选、视图和提醒
- 增强“待处理事项”与“下一步建议”

### 阶段三：能力扩展

目标：

- 接入更多资讯来源
- 接入更多内容模板
- 支持团队化协作和更丰富的知识库联动

## 12. 最终结论

本项目最合适的方向不是“继续堆 skill”，也不是“纯文件夹自动化”，而是：

`OpenClaw 作为前台聊天入口`

`飞书多维表格作为流程状态中台`

`飞书文档作为正文与成果承载`

`现有 ai-news skills 作为后台可复用能力模块`

这是当前最符合你需求、也最利于后续接入飞书知识库和长期演进的方案。
